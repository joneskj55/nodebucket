name: Awake Action
on:
  schedule:
    # every 55 minutes, the steps below are triggered
    - cron: "55 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: awake-action
        uses: devpolo/awake-action@v1
        with:
          url: "https://jones-nodebucket.herokuapp.com/"
